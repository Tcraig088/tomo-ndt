from setuptools import setup, find_packages

setup(
    name='tomondt',  # Replace with your package name
    version='0.1.0',  # Replace with your package version
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here
        # 'numpy',
        # 'pandas',
    ],
)